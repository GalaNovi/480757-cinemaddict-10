const extraFilmHeadings = [`Top rated`, `Most commented`];
const filters = [`all`, `watchlist`, `history`, `favorites`];
const emotions = [`smile`, `sleeping`, `puke`, `angry`];

export {extraFilmHeadings, filters, emotions};
